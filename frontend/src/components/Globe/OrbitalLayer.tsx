import React from 'react';
export const OrbitalLayer: React.FC = () => {
  // Helper component to mount effect driven routing
  // All Cesium entity logic was merged into CesiumGlobe for lifecycle simplicity
  // This component exists as a stub per requirements architecture
  return null;
};
